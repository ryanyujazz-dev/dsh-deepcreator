diff --git a/lib/client.js b/lib/client.js
index ea42b6b27ae432ec9e1d25c73357eae80aa43c92..4d517c06bd494e36b1d5be5ae5a39d3de63efa88 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -1795,6 +1795,20 @@ window.__ModuleLoader__.load({
 				this.openPromise = promise;
 				return promise;
 			}
+			/** Cool the live observer while retaining the last assembled presentation. */
+			deactivate() {
+				if (this.openState === "cold") return;
+				this.openGeneration++;
+				this.openPromise = null;
+				this.openState = "cold";
+				this.openError = null;
+				this.loadingOlder = false;
+				this.jumpPromise = null;
+				this.jumpTargetSeq = null;
+				const events = this.events;
+				this.events = void 0;
+				void events?.dispose();
+			}
 			/** Page up: pull one earlier page with the window's first seq as beforeSeq and prepend. */
 			async loadOlder() {
 				if (this.openState !== "open" || !this.hasMore || this.loadingOlder) return;
@@ -2307,6 +2321,14 @@ window.__ModuleLoader__.load({
 					};
 				}
 			}
+			/** Retain and configure a catalog-derived address without selecting it. */
+			retainNavigationAddress(sessionId) {
+				const address = this.navigationAddress(sessionId);
+				if (address === void 0) return;
+				this.addresses.set(sessionId, address);
+				this.sessions.get(sessionId)?.configureSubagent(address, this.catalogs.get(address.parentSessionId)?.parentAvailable ?? false);
+				return address;
+			}
 			/**
 			* Drop a session instance (scope-prune companion: instance
 			* and scope share one lifecycle). The host session log is the durable
@@ -3047,6 +3069,8 @@ window.__ModuleLoader__.load({
 			watched;
 			/** Removed-while-staged sessions whose teardown waits for the stage to move away. */
 			deferredRemovals = /* @__PURE__ */ new Set();
+			/** Session ids held observable by non-navigating embed leases (DeepCreator compat lease service). */
+			observationRetained = /* @__PURE__ */ new Set();
 			/**
 			* @param ctx - client root context (scope fibers mount under it).
 			* @param remote - generated Remote namespaces shared with every Session.
@@ -3297,6 +3321,18 @@ window.__ModuleLoader__.load({
 			binding(id) {
 				return this.resolve(id)?.binding;
 			}
+			/** Hold a session observable for a non-navigating lease; widened eligibility keeps its scope alive. */
+			retainObservation(id) {
+				this.observationRetained.add(id);
+			}
+			/** Release a non-navigating lease; prune may then reclaim the scope. */
+			releaseObservation(id) {
+				this.observationRetained.delete(id);
+			}
+			/** Retain a catalog-derived address for a non-navigating lease without selecting it. */
+			retainNavigationAddress(id) {
+				return this.manager.retainNavigationAddress(id);
+			}
 			/**
 			* Move the stage to the list's current session: sweep teardowns deferred
 			* behind the previous occupant and pull the new occupant's history window.
@@ -3353,7 +3389,7 @@ window.__ModuleLoader__.load({
 			/** The one aliveness predicate shared by scope mint and prune: host-listed or currently addressed. */
 			eligible(id) {
 				const { ids, current } = this.list.getSnapshot();
-				return current === id || ids.includes(id);
+				return current === id || ids.includes(id) || this.observationRetained.has(id);
 			}
 			/** Project the manager's list snapshot into the store (title derivation is display-only). */
 			projectList() {
diff --git a/lib/types/client/sessions/manager.d.ts b/lib/types/client/sessions/manager.d.ts
index f6f616a97b8a4a0ac73dcbc6a329461463e35889..ad3bfa7cce51b54dca94da5f11221cb318375e56 100644
--- a/lib/types/client/sessions/manager.d.ts
+++ b/lib/types/client/sessions/manager.d.ts
@@ -120,6 +120,8 @@ export declare class SessionManager {
      * @returns A retained or catalog-derived direct-parent address.
      */
     navigationAddress(sessionId: SessionId): SubagentAddress | undefined;
+    /** Retain and configure a catalog-derived address without selecting it. */
+    retainNavigationAddress(sessionId: SessionId): SubagentAddress | undefined;
     /**
      * Drop a session instance (scope-prune companion: instance
      * and scope share one lifecycle). The host session log is the durable
diff --git a/lib/types/client/sessions/service.d.ts b/lib/types/client/sessions/service.d.ts
index e6fb2c6044792e37f4c9381ca97265a5b740a733..ad51400d219dd92cafadb073947df4a1a050f401 100644
--- a/lib/types/client/sessions/service.d.ts
+++ b/lib/types/client/sessions/service.d.ts
@@ -310,6 +310,12 @@ export declare class ClientSessions implements ISessions {
      * @returns binding, or undefined for a session neither listed nor already scoped.
      */
     binding(id: SessionId): SessionBinding | undefined;
+    /** Hold a session observable for a non-navigating lease; widened eligibility keeps its scope alive. */
+    retainObservation(id: SessionId): void;
+    /** Release a non-navigating lease; prune may then reclaim the scope. */
+    releaseObservation(id: SessionId): void;
+    /** Retain a catalog-derived address for a non-navigating lease without selecting it. */
+    retainNavigationAddress(id: SessionId): SubagentAddress | undefined;
     /**
      * Move the stage to the list's current session: sweep teardowns deferred
      * behind the previous occupant and pull the new occupant's history window.
diff --git a/lib/types/client/sessions/session.d.ts b/lib/types/client/sessions/session.d.ts
index 0b41002afd6be2d2c1c548813d9f030692c77775..894b4f03a6d3a541e08fe7d1db3e852c41c049ee 100644
--- a/lib/types/client/sessions/session.d.ts
+++ b/lib/types/client/sessions/session.d.ts
@@ -194,6 +194,8 @@ export declare class Session implements SessionFace {
     }>>;
     /** First open: pull the tail page (idempotent — in-flight/already-open returns the existing promise). */
     open(): Promise<void>;
+    /** Release the assembled event window while keeping scoped presentation state resident. */
+    deactivate(): void;
     /** Page up: pull one earlier page with the window's first seq as beforeSeq and prepend. */
     loadOlder(): Promise<void>;
     /** Jump loader: page backwards until the window covers seq (see ISession.loadThrough). */
