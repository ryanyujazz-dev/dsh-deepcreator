diff --git a/lib/index.js b/lib/index.js
index e720306f4f9ce498fe127733e0c95e47ef259cf3..b7a0f27adc0042aa518bbdd06c0cdf46afb15129 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -501,7 +501,9 @@ function computeHunkDiffs(path, before, after) {
 		diffs.push({
 			path,
 			oldText: oldLines.length > 0 ? oldLines.join("\n") : null,
-			newText: newLines.join("\n")
+			newText: newLines.join("\n"),
+			oldStart: hunk.oldStart,
+			newStart: hunk.newStart
 		});
 	}
 	return diffs;
@@ -644,10 +646,12 @@ function applyWriteTool(ctx, sandbox) {
 				type: "text",
 				text: formatWriteOutput(value.path, value)
 			}],
-			presentationMeta: (args, value) => ({ diffs: value.before === null ? [] : computeHunkDiffs(args.file_path, value.before, value.after).map(({ path, oldText, newText }) => ({
+			presentationMeta: (args, value) => ({ diffs: value.before === null ? [] : computeHunkDiffs(args.file_path, value.before, value.after).map(({ path, oldText, newText, oldStart, newStart }) => ({
 				path,
 				oldText,
-				newText
+				newText,
+				oldStart,
+				newStart
 			})) })
 		},
 		async execute(args, exec) {
@@ -793,10 +797,12 @@ function applyEditTool(ctx, sandbox) {
 				type: "text",
 				text: formatEditOutput(value.path, args.replace_all ?? false)
 			}],
-			presentationMeta: (args, value) => ({ diffs: computeHunkDiffs(args.file_path, value.before, value.after).map(({ path, oldText, newText }) => ({
+			presentationMeta: (args, value) => ({ diffs: computeHunkDiffs(args.file_path, value.before, value.after).map(({ path, oldText, newText, oldStart, newStart }) => ({
 				path,
 				oldText,
-				newText
+				newText,
+				oldStart,
+				newStart
 			})) })
 		},
 		async execute(args, exec) {
@@ -1234,4 +1240,4 @@ function apply(ctx, config) {
 	applyEditTool(ctx, sandbox);
 }
 //#endregion
-export { Config, apply, inject, name };
+export { Config, apply, computeHunkDiffs, inject, name };
diff --git a/lib/types/index.d.ts b/lib/types/index.d.ts
index 3a246680d60d03c0eefd49a64eb14a7663ce6e77..98e407039a1589dd1a0e9b5011dbf730efac3c13 100644
--- a/lib/types/index.d.ts
+++ b/lib/types/index.d.ts
@@ -6,6 +6,7 @@
  */
 import type { Context } from '@deepseek-ai/cordis';
 import z from '@deepseek-ai/schemastery';
+export { computeHunkDiffs } from './diff.js';
 /** Cordis plugin name used by loader diagnostics. */
 export declare const name = "tool-fs";
 /** Services required by the filesystem tool suite. */
