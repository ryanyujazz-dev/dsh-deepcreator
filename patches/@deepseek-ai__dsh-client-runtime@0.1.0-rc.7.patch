diff --git a/lib/client.js b/lib/client.js
index 37b931e21184061c457a6b20ede16eff1a293087..3774332473d9ed071eb965095cb0106d3814e15e 100644
--- a/lib/client.js
+++ b/lib/client.js
@@ -282,7 +282,8 @@ window.__ModuleLoader__.load({
 					storeOf: (entry, scopeKey) => entry.store === void 0 ? void 0 : this.resolveStore(entry.store, scopeKey),
 					sessions: {
 						list: sessions.list,
-						provideInfo: sessions.currentProvideInfo
+						provideInfo: sessions.currentProvideInfo,
+						provideInfoFor: (id) => sessions.provideInfoFor(id)
 					},
 					workspaces: { list: workspaces.list },
 					get locale() {
@@ -7380,6 +7381,18 @@ Set the \`cycles\` parameter to \`"ref"\` to resolve cyclical schemas with defs.
 				this.openPromise = promise;
 				return promise;
 			}
+			/** Cool the live observer while retaining the last assembled presentation. */
+			deactivate() {
+				if (this.openState === "cold") return;
+				this.openGeneration++;
+				this.openPromise = null;
+				this.openState = "cold";
+				this.openError = null;
+				this.loadingOlder = false;
+				this.subscribedLastSeq = null;
+				this.liveBuffer = [];
+				this.stitching = false;
+			}
 			/** Page up: pull one earlier page with the window's first seq as beforeSeq and prepend. */
 			async loadOlder() {
 				if (this.openState !== "open" || !this.hasMore || this.loadingOlder) return;
@@ -7615,12 +7628,13 @@ Set the \`cycles\` parameter to \`"ref"\` to resolve cyclical schemas with defs.
 			installWindow(entries, hasMore, projections) {
+				const unchanged = entries.length === this.events.length && entries.every((entry, index) => entry.event.seq === this.events[index]?.seq);
 				this.events = entries.map((e) => e.event);
 				this.views = entries.map((e) => e.view);
 				this.baseSeq = this.events[0]?.seq ?? 0;
 				this.hasMore = hasMore;
 				if (this.events.some((event) => event.type === "turn/start")) this.firstPromptPendingTurn = false;
-				this.conversation.replaceWindow(entries.map(conversationInput), hasMore);
+				if (!unchanged) this.conversation.replaceWindow(entries.map(conversationInput), hasMore);
 				if (projections !== void 0) this.projections.seed(projections);
 				const buffered = this.liveBuffer;
 				this.liveBuffer = [];
 				for (const item of buffered) this.appendLive(item.event, item.view);
 				this.notifier.markDirty();
@@ -7915,6 +7929,14 @@ Set the \`cycles\` parameter to \`"ref"\` to resolve cyclical schemas with defs.
 					};
 				}
 			}
+			/** Retain a catalog-derived child address without selecting it. */
+			retainNavigationAddress(sessionId) {
+				const address = this.navigationAddress(sessionId);
+				if (address === void 0) return;
+				this.addresses.set(sessionId, address);
+				this.sessions.get(sessionId)?.configureSubagent(address, this.catalogs.get(address.parentSessionId)?.parentAvailable ?? false);
+				return address;
+			}
 			/**
 			* Drop a session instance (scope-prune companion: instance
 			* and scope share one lifecycle). The host session log is the durable
@@ -8879,6 +8901,8 @@ Set the \`cycles\` parameter to \`"ref"\` to resolve cyclical schemas with defs.
 			scopes = /* @__PURE__ */ new Map();
 			/** The provide channel (roster, materialization rules, current projection) — shared with the test runtime's double. */
 			provideChannel;
+			/** Explicit non-navigating render observers, created lazily by session id. */
+			explicitProvideSources = /* @__PURE__ */ new Map();
 			/**
 			* The staged session id — follows `list.current` exactly, holding its last
 			* defined value across masked gaps (a transiently absent selection blanks
@@ -8920,10 +8944,12 @@ Set the \`cycles\` parameter to \`"ref"\` to resolve cyclical schemas with defs.
 				this.list.subscribe(() => {
 					this.followCurrent();
 					this.provideChannel.publishCurrent();
+					this.refreshExplicitProviders();
 				});
 				this.provideChannel = new SessionProvideChannel({
 					rebuildBundles: () => {
 						for (const record of this.scopes.values()) record.provideInfo = this.provideChannel.materializeInfo(record.binding);
+						this.refreshExplicitProviders();
 					},
 					resolveCurrent: () => this.maybeProvideInfo(this.list.getSnapshot().current)
 				});
@@ -9152,6 +9178,90 @@ Set the \`cycles\` parameter to \`"ref"\` to resolve cyclical schemas with defs.
 				return this.resolve(id)?.provideInfo;
 			}
 			/**
+			* Observable render bundle for one explicit session id. Its subscription
+			* owns a non-navigating history-window lease.
+			*/
+			provideInfoFor(id) {
+				const existing = this.explicitProvideSources.get(id);
+				if (existing !== void 0) return existing.source;
+				const entry = {
+					listeners: /* @__PURE__ */ new Set(),
+					subscribers: 0,
+					releaseToken: 0,
+					snapshot: this.provideChannel.maybeInfo,
+					source: void 0
+				};
+				entry.source = {
+					getSnapshot: () => entry.snapshot,
+					subscribe: (fn) => {
+						if (this.explicitProvideSources.get(id) !== entry) this.explicitProvideSources.set(id, entry);
+						entry.listeners.add(fn);
+						entry.subscribers++;
+						entry.releaseToken++;
+						if (entry.subscribers === 1) this.refreshExplicitProvider(id, entry);
+						return () => {
+							if (!entry.listeners.delete(fn)) return;
+							entry.subscribers--;
+							if (entry.subscribers !== 0) return;
+							const token = ++entry.releaseToken;
+							queueMicrotask(() => {
+								if (entry.subscribers !== 0 || entry.releaseToken !== token) return;
+								this.coolExplicitProvider(id);
+								entry.snapshot = this.provideChannel.maybeInfo;
+							});
+						};
+					}
+				};
+				this.explicitProvideSources.set(id, entry);
+				return entry.source;
+			}
+			/** Keep only a small MRU of inactive assembled child tails. */
+			coolExplicitProvider(id) {
+				const entry = this.explicitProvideSources.get(id);
+				if (entry?.subscribers > 0 || this.list.getSnapshot().current === id) return;
+				this.scopes.get(id)?.session.deactivate();
+				if (entry === void 0) return;
+				this.explicitProvideSources.delete(id);
+				this.explicitProvideSources.set(id, entry);
+				let cold = 0;
+				for (const [candidate, source] of this.explicitProvideSources) if (source.subscribers === 0 && this.list.getSnapshot().current !== candidate) cold++;
+				if (cold <= 8) return;
+				for (const [candidate, source] of this.explicitProvideSources) {
+					if (cold <= 8) break;
+					if (source.subscribers > 0 || this.list.getSnapshot().current === candidate) continue;
+					this.explicitProvideSources.delete(candidate);
+					cold--;
+				}
+				this.pruneScopes();
+			}
+			explicitObserverCount(id) {
+				return this.explicitProvideSources.get(id)?.subscribers ?? 0;
+			}
+			refreshExplicitProviders() {
+				for (const [id, entry] of this.explicitProvideSources) {
+					if (entry.subscribers > 0) this.refreshExplicitProvider(id, entry);
+				}
+			}
+			refreshExplicitProvider(id, entry) {
+				const snapshot = this.list.getSnapshot();
+				const address = this.manager.retainNavigationAddress(id);
+				let next = this.provideChannel.maybeInfo;
+				if (snapshot.current === id || snapshot.ids.includes(id) || address !== void 0) {
+					const record = this.resolve(id);
+					if (record !== void 0) {
+						record.session.open();
+						next = record.provideInfo;
+					}
+				}
+				if (entry.snapshot === next) return;
+				entry.snapshot = next;
+				for (const fn of [...entry.listeners]) try {
+					fn();
+				} catch (error) {
+					console.error("sessions.provideInfoFor subscriber failed:", error);
+				}
+			}
+			/**
 			* Resolve the current-session-optional standard kit. Unknown or absent ids
 			* return the static no-session projection rather than removing hook props.
 			*/
@@ -9169,8 +9279,10 @@ Set the \`cycles\` parameter to \`"ref"\` to resolve cyclical schemas with defs.
 				const snapshot = this.list.getSnapshot();
 				const current = snapshot.current;
 				if (current === void 0 || snapshot.byId[current] === void 0 || current === this.watched) return;
+				const previous = this.watched;
 				this.watched = current;
 				this.sweepDeferred();
+				if (previous !== void 0 && this.explicitObserverCount(previous) === 0) this.coolExplicitProvider(previous);
 				const record = this.resolve(current);
 				/* v8 ignore next 3 -- defensive: current is always a listed id (open()
 				* validates and the projection masks absent selections), so resolve
@@ -9211,7 +9323,7 @@ Set the \`cycles\` parameter to \`"ref"\` to resolve cyclical schemas with defs.
 			/** The one aliveness predicate shared by scope mint and prune: host-listed or currently addressed. */
 			eligible(id) {
 				const { ids, current } = this.list.getSnapshot();
-				return current === id || ids.includes(id);
+				return current === id || ids.includes(id) || this.explicitProvideSources.has(id);
 			}
 			/** Project the manager's list snapshot into the store (title derivation is display-only). */
 			projectList() {
diff --git a/lib/types/client/contract/sessions.d.ts b/lib/types/client/contract/sessions.d.ts
index 0cd58417c1d7b888a384e3b4d9e26cd19a7f91f1..a305fa9c90b90b8f2aa39ce31146869bc182b811 100644
--- a/lib/types/client/contract/sessions.d.ts
+++ b/lib/types/client/contract/sessions.d.ts
@@ -22,6 +22,8 @@ export interface ISessions {
     readonly list: ObservableSnapshot<SessionListState>;
     /** Atomic current-session provide projection (the renderer host's `sessions.provideInfo` feed). */
     readonly currentProvideInfo: HostObservable<SessionMaybeProvideInfo>;
+    /** Explicit non-navigating session projection; subscribing owns the history-window lease. */
+    provideInfoFor(id: SessionId): HostObservable<SessionMaybeProvideInfo>;
     /**
      * The `session.search` result bound the wire schema fixes, exposed to
      * presentation as injected data. Not per-connection state: every transport
diff --git a/lib/types/client/sessions/manager.d.ts b/lib/types/client/sessions/manager.d.ts
index d53cc33a34f880e6771a5ac44cf1542f4cfb34da..c40359dda39a4471629351f7dd5c28032f5fcdf7 100644
--- a/lib/types/client/sessions/manager.d.ts
+++ b/lib/types/client/sessions/manager.d.ts
@@ -121,6 +121,8 @@ export declare class SessionManager {
      * @returns A retained or catalog-derived direct-parent address.
      */
     navigationAddress(sessionId: SessionId): SubagentAddress | undefined;
+    /** Retain and configure a catalog-derived address without selecting it. */
+    retainNavigationAddress(sessionId: SessionId): SubagentAddress | undefined;
     /**
      * Drop a session instance (scope-prune companion: instance
      * and scope share one lifecycle). The host session log is the durable
diff --git a/lib/types/client/sessions/service.d.ts b/lib/types/client/sessions/service.d.ts
index f421193c93bb67513167fe7474b6071f3e52717e..4892370adc453b54d1a527451b09e18b4adc01e4 100644
--- a/lib/types/client/sessions/service.d.ts
+++ b/lib/types/client/sessions/service.d.ts
@@ -177,6 +177,8 @@ export declare class SessionRuntime implements ISessions {
     private readonly scopes;
     /** The provide channel (roster, materialization rules, current projection) — shared with the test runtime's double. */
     private readonly provideChannel;
+    /** Explicit non-navigating render observers, keyed by session id. */
+    private readonly explicitProvideSources;
     /**
      * The staged session id — follows `list.current` exactly, holding its last
      * defined value across masked gaps (a transiently absent selection blanks
@@ -204,6 +206,8 @@ export declare class SessionRuntime implements ISessions {
      * @returns disposer removing the provider (already-materialized bundles keep their members until their scope drops).
      */
     provide(descriptor: SessionProvideDescriptor): () => void;
+    /** Explicit non-navigating session projection; subscribing owns the history-window lease. */
+    provideInfoFor(id: SessionId): HostObservable<SessionMaybeProvideInfo>;
     /**
      * Select a listed or retained catalog-addressed session as current.
      * @param id - listed or addressed session id.
@@ -352,6 +356,9 @@ export declare class SessionRuntime implements ISessions {
      * return the static no-session projection rather than removing hook props.
      */
     private maybeProvideInfo;
+    private explicitObserverCount;
+    private refreshExplicitProviders;
+    private refreshExplicitProvider;
     /**
      * Move the stage to the list's current session: sweep teardowns deferred
      * behind the previous occupant and pull the new occupant's history window.
diff --git a/lib/types/client/sessions/session.d.ts b/lib/types/client/sessions/session.d.ts
index cbc76a0a94dd9a88d99ec088e0d85990e59e0272..1b7bcc25596e0c8f0b774eeab5e30d681ea17c7b 100644
--- a/lib/types/client/sessions/session.d.ts
+++ b/lib/types/client/sessions/session.d.ts
@@ -187,6 +187,8 @@ export declare class Session implements SessionFace {
     }>>;
     /** First open: pull the tail page (idempotent — in-flight/already-open returns the existing promise). */
     open(): Promise<void>;
+    /** Release the assembled event window while keeping scoped presentation state resident. */
+    deactivate(): void;
     /** Page up: pull one earlier page with the window's first seq as beforeSeq and prepend. */
     loadOlder(): Promise<void>;
     /** Reconnect rebuild (manager calls this on onConnected for instances that were opened):
