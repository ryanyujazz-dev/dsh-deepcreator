diff --git a/lib/types/index.d.ts b/lib/types/index.d.ts
index 9b11dcbcb7ac41ca5c3765fceeebace4ccdc4efe..1177d91cc7f779c7aad8974e76f66429bf650280 100644
--- a/lib/types/index.d.ts
+++ b/lib/types/index.d.ts
@@ -257,6 +257,8 @@ export type MatchedShare<E extends SlotEntryDef, M> = E['kind'] extends 'chain'
 export type UseSession<Snap extends object = object> = SnapshotSelectorHook<Snap>;
 /** Props of the standard-kit SessionProvider seat (render-prop form). */
 export interface SessionAreaProps {
+    /** Explicit session to render without changing current selection. */
+    sessionId?: SessionIdOf | undefined;
     /** No-session body (also covers a current id whose session cannot be resolved). */
     empty?: (() => ReactNode) | undefined;
     /** Session body; the framework remounts it per session (key=sessionId). */
diff --git a/lib/types/renderer.d.ts b/lib/types/renderer.d.ts
index 2ec121dba8f6683b03aaba808aeefee1cc7d947a..dc9559650592e5e90a02eedf30b16c9c6667a43a 100644
--- a/lib/types/renderer.d.ts
+++ b/lib/types/renderer.d.ts
@@ -170,6 +170,8 @@ export interface SlotRendererHost {
          * undefined while no current session resolves.
          */
         provideInfo: HostObservable<SessionMaybeProvideInfo>;
+        /** Session-id-addressed provide source whose subscription owns a non-navigating view lease. */
+        provideInfoFor(id: string): HostObservable<SessionMaybeProvideInfo>;
     };
     /** Workspace-side standard-kit sources. */
     workspaces: {
