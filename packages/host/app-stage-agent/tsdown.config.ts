import { hostBundle } from '../../../scripts/tsdown.host.ts'

/** Build the app-stage agent preset-row plugin and emit its Typert artifacts. */
export default hostBundle('@ryanyujazz/dsh-app-stage-agent')
