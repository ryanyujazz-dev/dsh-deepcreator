# DeepCreator Review Service

Read-only Git repository facts for Workbench: status, one-file working-tree diff, and `git diff --check`. Every call re-resolves the canonical workspace/repository root and fences file paths to that repository. No stage, discard, commit, or mutation API exists.
