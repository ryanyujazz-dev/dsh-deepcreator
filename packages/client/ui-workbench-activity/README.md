# Workbench Activity provider

Registers the `activity` Workbench type and projects the current Session's official `jobsBySession` array directly. It owns only disposable selection/render state and never copies Job lifecycle data.
